/*Recepcion de evento de presion de todos los botones tipo deleted
usados para eliminar las prendas del carrito de compras*/
const Clickbutton = document.querySelectorAll('.delete')

/*iteracion por cada boton*/ 
Clickbutton.forEach(btn => {
    btn.addEventListener("click", removeItemCarrito)
})
/* funcion eliminar carrito
function removeItemCarrito(e){
    const buttonDelete = e.target  
    const tr = buttonDelete.closest(".producto")
    tr.remove()
    CarritoTotal
}
*/
/*Funcion para calcular el costo total de las prendas, itera sobre cada precio 
 de cada prenda, se activa cada vez que se elimina un articulo, se suma un articulo
 o se modifica la cantidad de producto, aun no esta en funcionamiento
function CarritoTotal(){
   let Total = 0;
    const itemCartTotal = document.querySelectorAll('.itemCartTotal')
    carrito.forEach((item) =>{
        const precio = Number(item.precio.replace("$", ""))
        Total = Total+precio*item.cantidad
    })
    itemCartTotal.innerHTML = `Total$${Total}`
}*/